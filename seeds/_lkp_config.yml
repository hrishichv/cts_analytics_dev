version: 2

seeds:
  - name: lkp_divisions
    config:
      schema: "{{env_var('DBT_LOOKUP_SCH', 'lookup_dev')}}"
  - name: lkp_categories
    config:
      schema: "{{env_var('DBT_LOOKUP_SCH', 'lookup_dev')}}"
  - name: lkp_shippers
    config:
      schema: "{{env_var('DBT_LOOKUP_SCH', 'lookup_dev')}}"